app:
  config:
    app-name: Sample

    
    
spring:
#  jpa:
#    database: POSTGRESQL
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/demo
    username: postgres
    password: postgres
